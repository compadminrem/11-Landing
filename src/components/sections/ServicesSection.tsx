import * as Icons from "lucide-react";
import type { ElementType } from "react";
import { ArrowRight, Flame, Sparkles } from "lucide-react";
import { Card } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { services } from "@/data/siteData";
export function ServicesSection() { return <section id="services" className="py-24"><div className="section-shell"><div className="mb-12 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="eyebrow mb-3">Поможем с любой проблемой</p><h2 className="text-4xl sm:text-5xl">Ремонтируем быстро.<br /><span className="text-primary">Объясняем понятно.</span></h2></div><p className="max-w-sm text-muted-foreground">Мастер назовёт точную стоимость и приступит к ремонту только после вашего согласия.</p></div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{services.map(([title,text,price,icon],i) => { const Icon = (Icons as Record<string,ElementType>)[icon] || Flame; return <Card key={title} className="group relative overflow-hidden p-6 transition-all hover:-translate-y-1 hover:border-primary/50 hover:shadow-xl"><span className="absolute right-5 top-5 text-4xl font-extrabold text-primary/10">0{i+1}</span><div className="mb-5 flex h-12 w-12 items-center justify-center rounded-2xl bg-primary/10 text-primary group-hover:bg-primary group-hover:text-white"><Icon size={23} /></div><h3 className="mb-2 text-2xl">{title}</h3><p className="mb-6 min-h-[52px] text-sm leading-6 text-muted-foreground">{text}</p><div className="flex items-center justify-between border-t pt-4"><span className="font-bold text-primary">{price}</span><Button variant="ghost" size="sm" asChild className="px-0 hover:bg-transparent hover:text-primary"><a href="#booking">Подробнее <ArrowRight size={15} /></a></Button></div></Card>})}</div><div className="mt-8 flex flex-col items-center justify-between gap-4 rounded-2xl bg-[#211a16] p-6 text-white sm:flex-row sm:p-8"><div className="flex items-center gap-4"><span className="flex h-12 w-12 items-center justify-center rounded-full bg-[#ffb72e] text-[#211a16]"><Sparkles size={21} /></span><div><strong className="block text-lg">Не нашли свою проблему?</strong><span className="text-sm text-white/60">Опишите симптомы — подскажем бесплатно</span></div></div><Button asChild className="rounded-xl bg-[#ffb72e] text-[#211a16] hover:bg-[#ffc95b]"><a href="#contacts">Задать вопрос</a></Button></div></div></section>; }
