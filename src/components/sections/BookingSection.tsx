import { useState } from "react";
import type { FormEvent } from "react";
import { CalendarDays, CircleCheck as CheckCircle2, Clock3 } from "lucide-react";
import { format } from "date-fns";
import { ru } from "date-fns/locale";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Calendar } from "@/components/ui/calendar";
import { Card } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { toast } from "@/hooks/use-toast";
import { timeSlots } from "@/data/siteData";
export function BookingSection() { const [date,setDate] = useState<Date|undefined>(new Date()); const [slot,setSlot] = useState(""); const [sent,setSent] = useState(false); const submit = (e: FormEvent<HTMLFormElement>) => { e.preventDefault(); if(!date || !slot){ toast({title:"Выберите дату и время",description:"Так мы сможем закрепить слот за вами."}); return; } setSent(true); toast({title:"Заявка принята",description:"Мастер свяжется с вами для подтверждения визита."}); }; return <section id="booking" className="bg-[#211a16] py-24 text-white"><div className="section-shell"><div className="mb-10 text-center"><p className="eyebrow mb-3 text-[#ffb72e]">Запись без ожидания</p><h2 className="text-4xl sm:text-5xl">Выберите удобное<br /><span className="text-[#ffb72e]">время визита</span></h2><p className="mx-auto mt-4 max-w-xl text-white/60">Оставьте контакты — мы подтвердим запись по телефону.</p></div>{sent ? <Card className="mx-auto max-w-2xl border-[#ffb72e]/30 bg-white/5 p-10 text-center text-white"><CheckCircle2 className="mx-auto mb-5 text-[#ffb72e]" size={56} /><h3 className="mb-3 text-3xl">Спасибо, заявка отправлена</h3><p className="text-white/60">Ждём вас {date&&format(date,"d MMMM",{locale:ru})} в интервале {slot}. Скоро позвоним.</p></Card> : <form onSubmit={submit} className="grid gap-6 lg:grid-cols-[.9fr_1.1fr]"><Card className="border-white/10 bg-white/5 p-4 text-white sm:p-6"><div className="mb-4 flex items-center gap-2"><CalendarDays className="text-[#ffb72e]" size={18}/><strong>Дата визита</strong></div><Calendar mode="single" selected={date} onSelect={setDate} locale={ru} disabled={{before:new Date()}} className="mx-auto w-fit rounded-xl bg-white text-foreground"/></Card><Card className="border-white/10 bg-white/5 p-6 text-white"><div className="mb-6 flex items-center gap-2"><Clock3 className="text-[#ffb72e]" size={18}/><strong>Время и контакты</strong>{date&&<Badge className="ml-auto bg-[#ffb72e]/15 text-[#ffca55]">{format(date,"d MMMM",{locale:ru})}</Badge>}</div><div className="mb-5 grid grid-cols-2 gap-2 sm:grid-cols-3">{timeSlots.map(time=><button type="button" key={time} onClick={()=>setSlot(time)} className={`rounded-lg border px-2 py-3 text-xs font-semibold ${slot===time?"border-[#ffb72e] bg-[#ffb72e] text-[#211a16]":"border-white/10 bg-white/5 text-white/70"}`}>{time}</button>)}</div><div className="grid gap-4 sm:grid-cols-2"><div><Label className="mb-2 block text-white/70">Ваше имя</Label><Input required placeholder="Иван Иванов" className="border-white/10 bg-white/5 text-white"/></div><div><Label className="mb-2 block text-white/70">Телефон</Label><Input required type="tel" placeholder="+7 (___) ___-__-__" className="border-white/10 bg-white/5 text-white"/></div></div><div className="mt-4"><Label className="mb-2 block text-white/70">Что случилось?</Label><Textarea placeholder="Например: духовка не нагревается..." className="border-white/10 bg-white/5 text-white"/></div><Button type="submit" size="lg" className="mt-5 h-12 w-full rounded-xl bg-[#ffb72e] font-bold text-[#211a16] hover:bg-[#ffc95b]">Подтвердить запись</Button></Card></form>}</div></section>; }
