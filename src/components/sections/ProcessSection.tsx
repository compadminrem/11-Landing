import { ArrowDown } from "lucide-react";
import { Card } from "@/components/ui/card";
import { Progress } from "@/components/ui/progress";
import { images, steps } from "@/data/siteData";
export function ProcessSection() { return <section id="process" className="bg-secondary/50 py-24"><div className="section-shell grid items-center gap-12 lg:grid-cols-[.9fr_1.1fr]"><div><p className="eyebrow mb-3">Простой путь к исправной технике</p><h2 className="mb-6 text-4xl sm:text-5xl">Всего четыре шага —<br /><span className="text-primary">и снова можно печь</span></h2><p className="mb-8 text-muted-foreground">Мы берём все заботы на себя: от поиска причины до уборки рабочего места.</p><img src={images.technician} alt="Мастер в мастерской" className="h-[330px] w-full rounded-3xl object-cover" /></div><div className="space-y-4">{steps.map(([number,title,text],i) => <Card key={number} className="flex gap-5 border-none p-5 shadow-sm hover:translate-x-1"><span className="flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl bg-primary text-lg font-extrabold text-white">{number}</span><div className="flex-1"><h3 className="mb-1 text-xl">{title}</h3><p className="text-sm leading-6 text-muted-foreground">{text}</p></div>{i<3 && <ArrowDown className="hidden text-primary/40 sm:block" size={18} />}</Card>)}<div className="pt-3"><div className="mb-2 flex justify-between text-xs font-bold"><span>Скорость сервиса</span><span className="text-primary">96%</span></div><Progress value={96} className="h-2 bg-white" /></div></div></div></section>; }
